# Production Environment Configuration
# Copy this file to .env.prod and update the values

# Database Configuration
DB_PASSWORD=your-super-secure-database-password-here

# JWT Configuration
JWT_SECRET=your-super-secure-jwt-secret-key-here-make-it-long-and-random

# API URL (should be your domain)
API_URL=https://your-domain.com/api

# Optional: Email configuration (if you add email features later)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Optional: File storage (if you use cloud storage later)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1
# AWS_BUCKET=your-bucket-name
